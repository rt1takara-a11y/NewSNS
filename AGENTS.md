# AGENTS.md — Codex（および他のAIエージェント）向け開発ルール

このリポジトリは、**毎月すべてがリセットされるSNS**（仮称 NewSNS）の開発リポジトリです。
Codex など AI エージェントはここで作業する前に、必ず以下のルールに従ってください。
（内容は `CLAUDE.md` と同じ方針です。両AIが同じルールで動くようにしています。）

## 最重要ルール

1. **作業前に `docs/` を読む。** 特に `docs/concept.md`、`docs/requirements.md`、`docs/decisions.md`、`docs/development-log.md`。
2. **仕様を変えたら `docs/` も更新する。**
3. **作業終了時に `docs/development-log.md` にログを残す**（やったこと・決めたこと・次にやること）。
4. **重要な設計判断は `docs/decisions.md` に理由とともに記録する。**
5. **チャット履歴だけを引き継ぎ資料にしない。** 決定・仕様・進捗はすべて docs に残す。

## このプロダクトの核（絶対に守る設計）

- **継続する情報**（内部アカウント・認証との対応・利用停止状態・管理記録）と、**月ごとに消える情報**（公開プロフィール・公開ID・投稿・フォロー・いいね・返信）を必ず分ける。
- **内部IDを画面・URL・APIレスポンスに出さない。** 公開面は毎月変わる公開IDだけを使う。
- **利用者同士が前月の人を追跡できないようにする。** 公開IDは毎月新しく発行する。
- **「画面から消す処理」と「運営が保持する記録の削除処理」を分ける。**
- **利用停止はリセットをまたいでも継続する。**

## セキュリティ

- 秘密情報（パスワード・APIキー等）は**環境変数**で管理し、**GitHubにコミットしない**。
- `.env` 等は `.gitignore` で除外する。

## 作業の進め方

- `docs/roadmap.md` の順番に沿って進める。
- 大きな変更・新しい依存は `docs/decisions.md` に記録する。
- 迷ったら「リセット後に戻ってくるか」を優先する設計を選ぶ。
