# 開発ログ（Development Log）

作業内容と「次にやること」を残す。新しい記録を上に追記する。
作業を始める前に必ず読み、作業を終えたら必ず書く。

---

## 2026-09-13 — Codex / プロフィールのフォロー中・フォロワー一覧

- 所有者がログイン成功を報告。続いて人数と一覧の追加を了承したため、統合済みコードから `codex/profile-connections` を作成。作業開始時に残っていた接続確認ログを保持した。
- 自分と他者のプロフィールに人数ボタンと一覧ダイアログを追加。名前・アイコンから月次公開IDのプロフィールへ移動でき、閉じるボタン・Escapeに対応する。
- Supabase CLIで追加migrationを生成。認証済み閲覧者と対象者の両方から見える今月の人だけを返すRPCと、フォロワー検索用indexを追加。内部ID・メール・前月・停止・双方向ブロック対象を返さない。既存migrationや認証・保存処理は変更していない。
- 状態更新後の遅延応答を非表示とし、取得中や失敗を0人と誤表示しない。体験版は自分の操作した関係だけを表示する。
- 検証: `npm test` **33/33成功**（既存22件＋DB権限/公開範囲8件＋クライアントの遅延応答/体験版3件）、`npm run build`、`npx tsc --noEmit` 成功。既存の永続化・返信・いいね・フォロー・ブロック・月替わり・利用停止テストも全migration適用状態で成功。
- 仕様・判断理由・SQL適用手順を更新。追加のアプリ依存なし。CLIが作る `supabase/.temp/` はGit除外。
- 実環境のSQL適用、ブラウザでの新UI操作確認、マージ・デプロイは未実施。次は `20260913021456_profile_connections.sql` を所有者が適用する判断を行い、2アカウントで人数と一覧を確認する。SQL未適用の現状では実DBからの人数取得はできない。

## 2026-09-13 — Codex / RE:ME接続設定の再読み込み

- 所有者が `.env.local` を保存後、値を表示せず検査。URLが既存RE:MEプロジェクトと一致し、Publishable key形式、秘密キー形式でないこと、Git除外を確認した。キーの有効性や実認証の成功はこの検査だけでは判定していない。
- 以前の開発サーバーを停止し、設定を読み直して `127.0.0.1:3100` で再起動。Chromeに体験版ではないメール入力画面が表示されることを確認し、所有者がログインできるよう画面とサーバーを残した。
- 確認コードの送信・入力は所有者が行う。認証後のプロフィール確認、必要に応じたメールテンプレート確認が次の作業。デプロイや実DBの変更なし。

## 2026-09-13 — Codex / ローカルログイン状態の確認

- ユーザーからログイン不具合と過去のログイン情報リセットを依頼された。対象はMac上の `127.0.0.1:3100` と確認。
- ローカルサーバーへの接続が拒否されたため開発サーバーを起動。Chromeでメールアドレスが空のログイン画面を確認した。現在の接続先では既にログアウト状態で、保存済みセッションの削除は行っていない。
- `.env.local` の値を出力せず形式だけを確認。URL・Publishable keyは設定済みだが、URLは以前のRE:ME用Supabaseと異なる。接続先は変更していない。
- アカウント・投稿の削除、確認コード送信、デプロイは行っていない。利用者が再ログインできるよう、ローカルサーバーとログイン画面を残した。

## 2026-09-13 — Codex / 統合先の確認とプロフィールPRのマージ準備

- ユーザーからマージを指示されたため、GitHubのPRと最新リモート履歴を取得。
- 認証・保存ブランチはPR #4で `claude/chatgpt-conversation-o2ayo4` へ統合済み（`c8375ee`）。PR #2のhead `6423ede` を含み、両ブランチのファイル差分がないことを確認した。PR #2は統合理由を追記して重複としてクローズ。
- PR #3のベースを同じ統合先へ変更。プロフィールの実装コミットと検証済みソースに変更なし。GitHub上のレビュー・commit status登録はなし。
- このログをPR #3へ含め、head SHAを指定して通常のマージコミットで取り込む。ブランチは削除しない。最終的なマージ結果はPR #3の状態と統合先の履歴を参照。
- デプロイ、Supabase接続設定、実環境への書き込みは対象外。次は接続設定を安全に用意し、新画面の実アカウント確認を行う。

## 2026-09-13 — Codex / プロフィール機能のドラフトPR作成

- 作業ツリーがクリーンであること、PR #2のheadが基点の `6423ede` のままであること、同じブランチの既存PRがないことを確認。
- `codex/user-profiles` をGitHubへpushし、[PR #3](https://github.com/rt1takara-a11y/NewSNS/pull/3)をドラフトで作成。ベースは `codex/persistent-backend`（PR #2）とし、PR #1/#2の変更を保持した。
- 前回の実装コミット `49c1a8e` にコード変更なし。テスト22件・本番ビルド・型チェックの成功結果をPRへ記載し、差分の空白エラーがないことを再確認した。
- 次: 新画面の実Supabase接続、ブラウザ操作、実機での確認。投稿は引き続き全体最新100件の範囲。接続設定値はチャット/Gitへ記載しない。
- マージ・デプロイ・SupabaseやSitesの設定変更は行っていない。PR #2のマージ後にPR #3のベース変更を検討する。

## 2026-09-12 — Codex / 他ユーザーの今月のプロフィール

### 引き継ぎ確認
- AGENTS.md、docs全体、現在のブランチ、PR #1/#2の説明・変更差分を確認。どちらも未マージのドラフトで、#2は#1を土台としている。
- 開始時の作業フォルダsmall-team-ai-notesは別サイト。既存ファイルを保持し、その中のNewSNSへリポジトリを新規取得。PR #2の最新 `6423ede` を基点にローカルブランチ `codex/user-profiles` を作成した。既存ブランチの変更・マージ・デプロイは行っていない。
- 所有者の確認済み事項: メール確認コードによるログイン、再読み込み後の投稿保持、同じアカウントの別端末での共有、別アカウントの投稿閲覧。以下のローカル検証とは区別する。

### やったこと・決めたこと
- 投稿・返信・ユーザー一覧の名前とアイコンから `/user/?id=<月次公開ID>` を開く導線を追加。
- 名前・絵文字アイコン・自己紹介・今月の投稿・フォロー/解除を表示。自分の公開プロフィールには編集画面への導線を表示。
- 既存の認証済み `reme_state` / `reme_mutate` を利用。テーブル・migration・認証・保存処理の変更なし。
- 実接続時は画面を開く際に再取得が成功するまで内容を表示しない。取得失敗、存在しないID、旧月のID、双方向ブロックで表示できない相手はプロフィールを出さない。URLの入力値を名前として表示せず、非公開の対応情報へフォールバックしない。
- 投稿は既存APIの全体最新100件のうち、その人の投稿を新着順で表示。画面にもこの上限を明記した。個人別の全件取得・ページングは今回含まない。
- ローカルの `.env.local` 等とプロセス環境の公開設定3項目は未設定。対象RE:ME Sitesのランタイム環境変数も登録なし（値は出力していない）。静的exportでは公開設定がビルド時に埋め込まれるため、ランタイム未登録は実サイト未接続を意味しない。既存公開サイトの設定・公開範囲は変更していない。

### 検証
- `npm test`: 22/22 PASS。既存の認証権限・保存・再起動・月次リセット試験、フォロー/解除保存、公開フィールド限定、プロフィール選択・ブロック・旧ID・未指定ID・空の投稿一覧を検証。
- `npm run build`: PASS、TypeScript PASS。`/user` を含むstatic export成功。
- ローカル `/user/?id=u_haru01` のHTTP 200を確認し、Codex内プレビューを開く要求を送信（UI応答はqueued）。ブラウザによるクリック操作・目視検証は未実施。
- 設定値のない体験版でのビルド。実環境への変更や実メール送信は行わず、新画面の実Supabase接続検証は未実施。
- 依存追加・lockfile変更なし。秘密情報はチャット/Gitへ記載していない。

### 次にやること
- 接続設定をローカルの無視対象ファイルに安全に設定し、実アカウントで新画面の遷移・再読み込み・フォロー解除・相互ブロックを確認する。
- 個人別に今月の投稿を全件表示する必要があれば、専用RPCとページングを別変更で設計する。
- 所有者の指示後にレビュー・マージ・再ビルド・デプロイを実施する。

## 2026-09-08 (4) — Codex / メール認証と永続化の実装

### やったこと
- SupabaseメールOTPログイン、PostgreSQL privateスキーマ、認証済み状態取得/更新RPCを実装。
- 投稿・返信・いいね・フォロー・プロフィール保存を接続。入力制限、操作UUIDによる再送重複防止、保存失敗時の入力保持を追加。
- DBの日本時間による月次境界、新公開プロフィール発行、旧期間からの書き込み拒否、月末画面更新を実装。
- 投稿通報・ブロック解除・投稿非表示、運営専用の停止/リセット/期限切れ保守SQLを追加。
- Supabase未接続時は明示した体験版。ライブ側の一般利用者にはリセットボタンを出さない。
- 接続、SMTP、保持方針、運営操作の手順をbackend-setup.mdへ記録。

### 検証
- `npm test`: PGlite上の実SQLテスト15/15 PASS。匿名拒否、内部データ遮断、本人性、入力制限、操作再送、双方向ブロック、証拠保全、月替わり、停止継続、書込制限、DB再起動後の保存を検証。
- `npm run build`: PASS、TypeScript PASS。
- Supabase実プロジェクト、メール配信、実認証を使った別端末の確認は未実施。ローカルSQL試験は実SMTP/実JWT連携の代替とは扱わない。

### 残る作業
- 所有者がSupabaseプロジェクトを用意しmigrationを適用、SMTP・公開設定値を設定する。
- 接続後に再ビルドし、2アカウントのメール認証と投稿共有を確認する。
- 一般公開前に規約、運営者情報、問い合わせ、ホストの閲覧範囲を確定する。
- 新しい実装は専用の機能ブランチでレビュー。現在の本人限定プレビューは体験版のまま維持する。

## 2026-09-08 (3) — Codex / RE:MEへ名称変更

### やったこと
- サービス名をRE:ME（リミー）に統一。ナビのロゴ、アクセシブル名、ページタイトル・説明、README、コンセプト資料、AI向けルールの名称を更新。
- 既存の本人限定プレビューとUI変更PRに反映する。

### 検証
- 本番ビルド・TypeScriptチェック成功。生成HTMLのタイトルとブランドを確認。

### 次にやること
- スマホ実機での表示確認、PRレビュー。本番の認証・DBは未実装。

## 2026-09-08 (2) — Codex / プレビュー検証

### やったこと
- 依存導入が成功し、Next.js本番ビルドとTypeScriptチェックが通過。
- ブラウザで4画面を開き、投稿・いいね・返信・フォロー・プロフィール保存・月初リセット・名前再設定・新しい人の取得を確認。
- PCのタイムライン表示を目視確認。専用のモバイル幅テストは未実施。
- Next.jsを維持してプレビュー起動用の引数変換ラッパーを追加。
- 操作可能な本人限定プレビューのため静的exportを有効化。データはブラウザ内メモリのみ。

### 次にやること
- スマホ実機でレイアウト・タップ操作を確認する。
- PRレビュー後にマージを判断する。本番の認証・DBは未実装。

## 2026-09-08 — Codex

### やったこと
- タイムライン・さがす・プロフィール・再スタートをチャコール×ライムのUIへ更新。
- PC用サイドナビと月表示の補助欄、スマホ用の下部ナビを実装。
- 共通の線画アイコンを追加。既存の絵文字アバターは維持。
- 投稿/返信ラベル、フォロー/タブ/アイコン選択状態、ナビの現在位置、プロフィール保存通知、フォーカス表示を改善。
- UIの公開ID表示を説明文へ変更。内部IDや永続化・リセットロジックの変更なし。

### 検証
- ソース差分をレビュー。相対・エイリアスimport参照とCSS波括弧の対応をスクリプトで確認（PASS）。CSS構文パーサーは未導入のため構文全体の検証は未実施。
- npm ci --offline --ignore-scripts を試行したが、環境のネットワーク承認キャンセルで依存導入が完了せず。Next.js/TypeScriptが未導入のためビルド・型チェック・ブラウザ確認は未実施。

### 次にやること
- Claude Code環境で npm ci と npm run build を実行する。
- 390px/1440pxで全4画面を確認し、投稿・いいね・返信・フォロー・プロフィール保存・リセット→再設定を試す。
- 内容を確認してUI変更ブランチをマージする。デプロイは未実施。

## 2026-09-07 (2) — Claude Code

### やったこと
- フェーズ1「画面の試作品」を作成した（Next.js 16 + TypeScript、ブラウザ内モックデータ・保存なし）。
  - `/`（タイムライン: 新着／フォロー中、投稿・いいね・返信）
  - `/discover`（さがす: 今月の人を見つけてフォロー。前月の人は追跡できない旨を明示）
  - `/profile`（今月の名前・アイコン・ひとことを設定、自分の投稿一覧）
  - `/restart`（月初リセットのシミュレーション → リセット後の再スタート画面。新しい公開IDを発行）
  - 共通: 下部タブバー、モバイル前提のスタイル、ダークモード対応
- `npm run build` 成功（型チェック通過）、`npm start` で全ルート 200 を確認。
- スマホ表示のスクリーンショットを取得しユーザーに共有。

### 決めたこと
- 試作品の技術スタックは Next.js（App Router）+ TypeScript。`decisions.md` に追記。
- アイコンは絵文字ピッカーで代替（画像アップロードは後回し）。
- リセットと公開ID発行は試作品ではブラウザ内で擬似再現（本番はフェーズ2以降でDB・認証・定期実行に置換）。

### 次にやること
- ユーザーに試作品の流れを確認してもらい、画面・文言・体験の調整点を洗い出す。
- フェーズ2に進む場合: ホスティング・DB・認証サービスの選定（無料枠・条件を確認）。

### 引き継ぎメモ
- `next@16` を採用（`next@15.1.6` は CVE のためパッチ版へ更新済み）。
- データは未永続。ページ再読み込みで初期状態に戻る（`src/lib/mockData.ts`）。

---

## 2026-09-07 — Claude Code

### やったこと
- 空だったリポジトリに、事業仕様とAI開発ルールのドキュメント一式を作成した。
  - `docs/concept.md` — コンセプト・提供価値・検証したいこと
  - `docs/requirements.md` — MVP要件・毎月リセットのルール
  - `docs/architecture.md` — 構成・データの分け方
  - `docs/roadmap.md` — 作る順番
  - `docs/decisions.md` — 決定事項と理由
  - `docs/development-log.md` — この開発ログ
  - `CLAUDE.md` / `AGENTS.md` — AI向け開発ルール
  - `README.md` — リポジトリの入口
  - `.gitignore` — 秘密情報・生成物を除外

### 決めたこと
- `docs/decisions.md` を参照（公開IDは運営内部のみ管理／継続アカウントと月次プロフィールの分離 など）。

### 次にやること
- ホスティング・DB・認証の具体サービスを選定する（無料枠・条件を確認）。
- フェーズ1（画面の試作品）に着手するか判断する。技術スタック（例: Next.js + TypeScript など）を決める。

### 引き継ぎメモ
- リセットのタイムゾーンは未確定（まず JST 想定）。
- アイコンの扱い（画像アップロードは後回し。初期はプリセット/絵文字で代替する案）。
- 利用者による他ユーザー検索を入れるかは、再特定防止の方針と衝突しうるため要検討。

---

## テンプレート（コピーして使う）

## YYYY-MM-DD — 誰が（Claude Code / Codex / 人）

### やったこと
-

### 決めたこと
-

### 次にやること
-

### 引き継ぎメモ
-


## 2026-09-08 — Codex（Supabase実環境作成）

- ユーザー指定の rt1takara-a11y's Org に RE:ME を作成。東京 ap-northeast-1、作成時の月額見積もり0。プロジェクトID: jcvfwpnerxubmtfxgehs。
- 初期migration reme_initialを実環境へ適用済み。9テーブルでRLS有効、authenticatedからprivateスキーマへのアクセス不可、anonからRPC実行不可を確認。
- 実DBで一時的なテストユーザーを使い、プロフィール設定・投稿RPCを実行しpost_saved=trueを確認。トランザクションをrollbackしテストデータは残していない。実メール認証のテストではない。
- Security advisor: privateのポリシー無し9件は直接アクセス禁止の設計によるINFO。認証済みユーザー向けSECURITY DEFINER RPC2件はWARN。認証・所有権検査、空search_path、公開フィールド制限を確認した上で意図的に維持。参照: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable および https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy
- 次: Authの確認コードメールテンプレート、SMTP、フロントの環境設定と再ビルド・デプロイ、実メール/別端末テスト、定期削除ジョブ。現在のSitesプレビューは引き続きデモ。
- Supabase連携にはAuth/SMTP設定変更機能がないため、メール設定は所有者のダッシュボード操作が必要。APIキーはログ/Gitに保存していない。
