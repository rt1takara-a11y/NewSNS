# CLAUDE.md — Claude Code 向け開発ルール

このリポジトリは、**毎月すべてがリセットされるSNS**（仮称 NewSNS）の開発リポジトリです。
Claude Code はここで作業する前に、必ず以下のルールに従ってください。

## 最重要ルール

1. **作業前に `docs/` を読む。** 特に `docs/concept.md`（何を作るか）、`docs/requirements.md`（要件）、`docs/decisions.md`（決定事項と理由）、`docs/development-log.md`（前回の続き）。
2. **仕様を変えたら `docs/` も更新する。** コードだけ変えて仕様を放置しない。
3. **作業終了時に `docs/development-log.md` にログを残す。** 「やったこと・決めたこと・次にやること」を書く。
4. **重要な設計判断をしたら `docs/decisions.md` に理由とともに記録する。**
5. **チャット履歴だけを引き継ぎ資料にしない。** 決定・仕様・進捗はすべて docs に残す。

## このプロダクトの核（絶対に守る設計）

- **継続する情報**（内部アカウント・認証との対応・利用停止状態・管理記録）と、**月ごとに消える情報**（公開プロフィール・公開ID・投稿・フォロー・いいね・返信）を必ず分ける。
- **内部IDを画面・URL・APIレスポンスに出さない。** 公開面は毎月変わる公開IDだけを使う。
- **利用者同士が前月の人を追跡できないようにする。** 公開IDは毎月新しく発行する。
- **「画面から消す処理」と「運営が保持する記録の削除処理」を分ける。**（通報対応のため一部記録は保持）
- **利用停止はリセットをまたいでも継続する。**

## セキュリティ

- パスワード・APIキーなどの秘密情報は**環境変数**で管理し、**GitHubには絶対にコミットしない**。
- `.env` 等の秘密ファイルは `.gitignore` で除外する。

## 作業の進め方

- まだ技術スタックが未確定のフェーズでは、`docs/roadmap.md` の順番に沿って進める。
- 大きな変更や新しい依存の追加は、`docs/decisions.md` に記録する。
- 迷ったら、投稿数より「リセット後に戻ってくるか」を優先する設計を選ぶ。
